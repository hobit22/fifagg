const { sequelize, Sequelize : { QueryTypes } } = require('./index');
const logger = require('../lib/logger');

const pushDb = {
	/** 매치 종류 메타데이터 */
	pushMatchDb : async function( matchtype, desc ){
		
	},
	
};